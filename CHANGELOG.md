# v0.0.1

## external `--!strict` compatibility added
Initially, using the methods provided by this module would most likely cause `Type Error: type is too complex to typecheck...` in `--!strict` mode, well now it doesn't!

Other than that, the README was improved a lot.

# v0.0.0
Initial public release